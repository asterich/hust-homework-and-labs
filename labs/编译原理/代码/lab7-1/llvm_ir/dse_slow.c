#include <stdio.h>long long compute(long long n) {    long long sum = 0;    for (long long i = 0; i < n; i++) {        long long tmp1 = i * 2;        long long tmp2 = tmp1 + 1;        long long tmp3 = tmp1 + tmp2; // This is essentially 3*i        sum += tmp3;    }    return sum;}int main() {    long long n = 100000000;    long long result = 0;    for (long long i = 1; i < 100; ++i) {        result = compute(n);    }    printf("Result: %lld\n", result);    return 0;}